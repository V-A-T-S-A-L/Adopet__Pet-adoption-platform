document.addEventListener('DOMContentLoaded', function() {
    // Example: Dynamically change the pet's image
    const petPhoto = document.getElementById('petPhoto');
    petPhoto.src = "bella.jpg"; // Change this path to dynamically load different images
});
<textarea id="reason" name="reason" placeholder="Why do you want to adopt a dog/puppy?" required></textarea>
